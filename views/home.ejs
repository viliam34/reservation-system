<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home</title>
  <link rel="stylesheet" type="text/css" href="/styles/login.css">
  <link rel="stylesheet" type="text/css" href="/styles/dashboard.css">
</head>
<body>
  <div class="home-bg-container" style="background: #f4f4f9; min-height: 60vh; margin: 40px auto; padding: 40px 0; display: flex; justify-content: center; align-items: center; border-radius: 24px; max-width: 700px;">
    <div class="home-selection-box" style="padding: 2.5rem 2rem; text-align: center; min-width: 440px; max-width: 600px; width: 100%;">
      <h1 style="color: #007bff; margin-bottom: 1.5rem;">Welcome</h1>
      <p style="margin-bottom: 2rem; color: #444;">Please select an option:</p>
      <div style="display: flex; justify-content: center; gap: 1rem; margin-bottom: 2rem;">
        <form method="GET" style="margin: 0;">
          <button type="submit" name="form" value="login" class="home-btn" style="padding: 0.8rem 2rem; background: #007bff; color: #fff; border-radius: 6px; text-decoration: none; font-size: 1.1rem; font-weight: 500; border: none; cursor: pointer; transition: background 0.3s;">Login</button>
        </form>
        <form method="GET" style="margin: 0;">
          <button type="submit" name="form" value="register" class="home-btn" style="padding: 0.8rem 2rem; background: #6c63ff; color: #fff; border-radius: 6px; text-decoration: none; font-size: 1.1rem; font-weight: 500; border: none; cursor: pointer; transition: background 0.3s;">Register</button>
        </form>
      </div>
      <% if (typeof form !== 'undefined' && form === 'login') { %>
        <div style="margin-top: 1.5rem;">
          <%- include('includes/login') %>
        </div>
      <% } else if (typeof form !== 'undefined' && form === 'register') { %>
        <div style="margin-top: 1.5rem;">
          <%- include('includes/register') %>
        </div>
      <% } %>
      <% if (typeof success !== 'undefined') { %>
        <div style="color: #28a745; margin-top: 1rem; font-weight: 500;"> <%= success %> </div>
      <% } %>
      <% if (typeof errors !== 'undefined' && errors.length > 0) { %>
        <div style="color: #dc3545; margin-top: 1rem; font-weight: 500;">
          <% errors.forEach(function(error) { %>
            <div><%= error %></div>
          <% }) %>
        </div>
      <% } %>
    </div>
  </div>
</body>
</html>
